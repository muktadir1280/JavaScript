//using concat method 1
const first = 'Muktadir';
const last = 'Rahman';
const fullName1 = first + last;
const fullName = first + ' ' + last; //using concat
console.log(fullName);

//using concat method 2
const first1 = 'Muktadir';
const last1 = 'Rahman';
fullName2 = first.concat(' ').concat(last); //using multiple concat
console.log(fullName);