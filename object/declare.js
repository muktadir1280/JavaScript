const pen = {
    brand: 'econo',
    price: 10,
    color: 'black'
}

const pencil = new Object() //another method of object declare
const rubber = Object.create({}) //another method of object declare